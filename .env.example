# Copy to .env and set a real secret key:
#   python3 -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=replace-me
DJANGO_DEBUG=1
# A leading dot matches any subdomain, which is what makes the randomly named
# quick tunnels usable without editing this file on every run.
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,.trycloudflare.com
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:8080,https://*.trycloudflare.com

POSTGRES_DB=planfc
POSTGRES_USER=planfc
POSTGRES_PASSWORD=change-me-too
